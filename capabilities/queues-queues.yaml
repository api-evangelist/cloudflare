naftiko: 1.0.0-alpha2
info:
  label: Cloudflare Queues API — Queues
  description: 'Cloudflare Queues API — Queues. 6 operations. Lead operation: Cloudflare List Queues. Self-contained Naftiko
    capability covering one Cloudflare business surface.'
  tags:
  - Cloudflare
  - Queues
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CLOUDFLARE_API_KEY: CLOUDFLARE_API_KEY
capability:
  consumes:
  - type: http
    namespace: queues-queues
    baseUri: https://api.cloudflare.com/client/v4
    description: Cloudflare Queues API — Queues business capability. Self-contained, no shared references.
    resources:
    - name: accounts-account_id-queues
      path: /accounts/{account_id}/queues
      operations:
      - name: listqueues
        method: GET
        description: Cloudflare List Queues
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createqueue
        method: POST
        description: Cloudflare Create Queue
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: accounts-account_id-queues-queue_id
      path: /accounts/{account_id}/queues/{queue_id}
      operations:
      - name: getqueue
        method: GET
        description: Cloudflare Get Queue
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: replacequeue
        method: PUT
        description: Cloudflare Replace Queue Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: updatequeue
        method: PATCH
        description: Cloudflare Update Queue Settings
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletequeue
        method: DELETE
        description: Cloudflare Delete Queue
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.CLOUDFLARE_API_KEY}}'
  exposes:
  - type: rest
    namespace: queues-queues-rest
    port: 8080
    description: REST adapter for Cloudflare Queues API — Queues. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/accounts/{account-id}/queues
      name: accounts-account-id-queues
      description: REST surface for accounts-account_id-queues.
      operations:
      - method: GET
        name: listqueues
        description: Cloudflare List Queues
        call: queues-queues.listqueues
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createqueue
        description: Cloudflare Create Queue
        call: queues-queues.createqueue
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/accounts/{account-id}/queues/{queue-id}
      name: accounts-account-id-queues-queue-id
      description: REST surface for accounts-account_id-queues-queue_id.
      operations:
      - method: GET
        name: getqueue
        description: Cloudflare Get Queue
        call: queues-queues.getqueue
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: replacequeue
        description: Cloudflare Replace Queue Configuration
        call: queues-queues.replacequeue
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updatequeue
        description: Cloudflare Update Queue Settings
        call: queues-queues.updatequeue
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletequeue
        description: Cloudflare Delete Queue
        call: queues-queues.deletequeue
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: queues-queues-mcp
    port: 9090
    transport: http
    description: MCP adapter for Cloudflare Queues API — Queues. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: cloudflare-list-queues
      description: Cloudflare List Queues
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: queues-queues.listqueues
      outputParameters:
      - type: object
        mapping: $.
    - name: cloudflare-create-queue
      description: Cloudflare Create Queue
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: queues-queues.createqueue
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cloudflare-get-queue
      description: Cloudflare Get Queue
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: queues-queues.getqueue
      outputParameters:
      - type: object
        mapping: $.
    - name: cloudflare-replace-queue-configuration
      description: Cloudflare Replace Queue Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: queues-queues.replacequeue
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cloudflare-update-queue-settings
      description: Cloudflare Update Queue Settings
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: queues-queues.updatequeue
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: cloudflare-delete-queue
      description: Cloudflare Delete Queue
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: queues-queues.deletequeue
      outputParameters:
      - type: object
        mapping: $.
